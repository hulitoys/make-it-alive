# Make It Alive

Make It Alive 把日常照片里的水杯、花草、石头或其他物件原地变成原创精灵，并把未经修改的原景与同场景插画排成一页观察图。

## 它会做什么

- 自动选择照片中最有辨识度和精灵化潜力的一个物体，也支持你直接点名。
- 保留原物体的轮廓、色彩或材质、结构细节，再将它们简化成清楚的精灵身体语言。
- 内部生成五个原创混成名候选并自行选定最终名字，不把候选交给用户挑选；默认不使用“××兽”或“××精灵”格式。
- 每张输入默认只调用一次图片生成：在同一轮里完成精灵设计、移除原物和原位替换，生成保持原视角与场景地标的 Make It Alive 插画。
- 使用明亮水粉、彩铅和卡牌插画般的环境叙事，避免粗黑描边、阴郁灰调、塑料 3D 和过度精修。
- 用确定性脚本排成有层次的编辑图鉴页，并从插画中提取场景专属强调色；右侧固定包含名字、通过口语与逻辑检查的性格和爱好，以及一条无标题的小字简介。不会出现“观察记录”栏目标题。

每张输入照片只对应一张 2400×1600 PNG 成品：左侧为完整原照片 A，右侧为同场景 Make It Alive 插画 B、名字、性格、爱好和小字简介。输入 N 张照片时，按输入顺序直接交付 N 张排版成品；不会先给候选图、单独的 B 图，也不会要求用户挑选更喜欢的版本。

## 安装

在 Codex 中使用 `$skill-installer`，让它从下面的 Skill 路径安装：

```text
https://github.com/hulitoys/make-it-alive/tree/main/skills/make-it-alive
```

也可以把 `skills/make-it-alive` 复制到个人 Skill 目录：

```text
$HOME/.agents/skills/make-it-alive
```

## 运行环境

在 ChatGPT 中请使用 **Work 模式**，或在 **Codex 任务**中安装并调用本 Skill。普通 Chat 模式适合讨论和构思，但不保证加载自定义 `SKILL.md`、执行本地 Python/Pillow 脚本或交付最终排版文件，因此不作为本 Skill 的完整运行环境。

## 使用

附上一张日常场景照片，然后输入：

```text
使用 $make-it-alive 处理我上传的照片，直接输出最终排版成品。
```

也可以一次附上多张照片；Skill 会逐张处理并输出相同数量的最终排版成品。

也可以指定物体：

```text
使用 $make-it-alive，把照片里的蓝色水杯原地变成一只精灵。
```

Skill 在 Codex 中默认使用内置 ImageGen，不需要 API Key。其他智能体需要具备等价的单次图生图能力，并能执行 Python/Pillow 排版脚本；GitHub 上的 Skill 本身不包含图片模型。Skill 已内置采用 SIL Open Font License 1.1 的 Noto Sans CJK SC，排版时不需要用户上传或临时下载中文字体；仍可用 `--font` 主动指定其他字体。

## 速度

默认快速路径对每张输入只生成一次图片，排版通常只需几秒。以本项目本地实测推算，单张目标总耗时约 40–60 秒；图片模型排队、平台限流和硬件状态可能让实际时间更长，因此一分钟是优化目标，不是硬性保证。只有首张结果出现原物未移除、位置严重错误、乱码或明显 3D/阴郁画风时，才追加一次纠正生成。多张输入在运行端允许时可以并行处理，但必须按原输入顺序交付。

为减少等待，运行端应为每张输入只请求一张约 1K 级别、与原图同比例的输出，不默认生成多个候选或 2K/4K 大图。若在豆包/火山引擎上运行，需要先把本 Skill 接到支持参考图输入的 Seedream 图生图工具；仅把仓库地址发给不支持 `SKILL.md`、图生图或本地 Python 的聊天端，并不能自动完成整套流程。通过火山引擎 API 接入时还需要自行配置 API Key，并会产生对应模型调用费用。

## 隐私与版权

- 原照片用于锁定显灵场景的构图并作为排版输入，源文件不会被覆盖、裁剪或滤镜处理。
- 默认不会把私人照片或生成结果提交到本仓库。
- 精灵设计必须原创；只借鉴收集养成类精灵作品的清晰剪影和卡牌场景感，不复制现有角色、卡面、品牌图标或属性系统。

## License

MIT
